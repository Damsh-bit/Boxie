import { NotFoundContent } from '@/ui/NotFoundContent'

/** Una ficha o página del sitio que no existe (con la barra y el pie del sitio). */
export default function MarketingNotFound() {
  return (
    <div className="bg-[linear-gradient(180deg,#fff0f3_0%,#ffffff_55%)]">
      <NotFoundContent />
    </div>
  )
}
